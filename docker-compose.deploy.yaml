version: "3.8"

services:
    temp_container:
        image: myapp-app-cached
        build:
            context: ./
            dockerfile: ./Dockerfile
        volumes:
          - .:/app
        environment:
          - TOKEN=${TOKEN}
        entrypoint: ./entrypoint.sh
